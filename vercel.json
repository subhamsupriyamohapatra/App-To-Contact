{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/contact.js" }
  ],
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
